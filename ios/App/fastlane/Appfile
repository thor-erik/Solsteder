app_identifier("app.findshades")
team_id("KG3KK5HF42")
# Apple-account auth is handled by the App Store Connect API key in the Fastfile
# (app_store_connect_api_key), so no apple_id / itc_team_id needed here.
